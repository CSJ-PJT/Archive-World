import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('core district mode is actual WebGL GLTF review',()=>{const main=fs.readFileSync(new URL('../src/main.ts',import.meta.url),'utf8');const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(main,/mode'\)==='core3d'/);assert.match(view,/new THREE\.WebGLRenderer/);assert.match(view,/GLTFLoader/);assert.match(view,/loadAsync/);assert.match(view,/new THREE\.InstancedMesh/);assert.match(view,/mergeGeometries/);assert.match(view,/ACTUAL_GLTF_MATERIAL_BATCH/);assert.match(view,/renderer\.info\.render/);assert.match(view,/manifest\.badges/);});
test('core stream mode loads actual stream GLB',()=>{const main=fs.readFileSync(new URL('../src/main.ts',import.meta.url),'utf8');const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(main,/corestream/);assert.match(main,/CORE_STREAM_BASE_URL/);assert.match(view,/manifest\.urbanStream\.uri/);assert.match(view,/Urban Stream/);assert.match(view,/archive-water-plaza/);assert.match(view,/future-riverfront-corridor/);assert.match(view,/reviewLod/);assert.match(view,/warmupReset/);});
test('core stream final mode adds lights, cameras and bounded performance mode',()=>{const main=fs.readFileSync(new URL('../src/main.ts',import.meta.url),'utf8');const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(main,/corestreamfinal/);assert.match(main,/CORE_STREAM_FINAL_BASE_URL/);assert.match(view,/viewerLights/);assert.match(view,/water-closeup/);assert.match(view,/performanceMode\?\.28/);assert.match(view,/powerPreference:'high-performance'/);assert.match(view,/v11-stream-final/);});
test('final review replaces legacy white infrastructure defaults with district hierarchy',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/finalizeInfrastructureMaterials/);assert.match(view,/tree-crown/);assert.match(view,/road-asphalt/);assert.match(view,/calibrated material hierarchy/);});
test('precision review preserves district streets without overlapping Hero water',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/reviewHeroCamera/);assert.match(view,/stays present for aerial\/context cameras/);assert.match(view,/if\(!reviewHeroCamera\)\{const infra=/);});
test('precision hero mode opens the stream ground plane and loads revisioned actual hero GLB',()=>{const main=fs.readFileSync(new URL('../src/main.ts',import.meta.url),'utf8');const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(main,/coreprecision/);assert.match(main,/CORE_PRECISION_BASE_URL/);assert.match(view,/heroZones/);assert.match(view,/new THREE\.PlaneGeometry\(1600,650\)/);assert.match(view,/hero-archive-axis/);assert.match(view,/hero-s-street-axis/);assert.match(view,/true 1\.65m eye height/);assert.match(view,/position:\[-340,3\.95,-15\]/);assert.match(view,/position:\[-350,4\.35,-16\],target:\[-244,3\.35,3\]/);assert.match(view,/position:\[-230,3\.95,-28\],target:\[-250,3\.5,0\]/);assert.match(view,/unobstructed 1\.65m eye-level composition/);assert.match(view,/heroAssetRevision/);assert.match(view,/zone\.geometryRevision/);assert.match(view,/zone\.geometry\?\.triangles/);assert.match(view,/finalizeHeroMaterials/);});
test('precision hero review separates cinematic evidence from performance measurement',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');const style=fs.readFileSync(new URL('../src/style.css',import.meta.url),'utf8');assert.match(view,/cinematicMode=!performanceMode/);assert.match(view,/new GTAOPass/);assert.match(view,/new UnrealBloomPass/);assert.match(view,/if\(composer\)composer\.render/);assert.match(view,/query\.get\('pose'\)/);assert.match(view,/heroFrontageLights/);assert.match(view,/heroRouteLights/);assert.match(view,/heroPromenadeLights/);assert.match(view,/heroBridgeLights/);assert.match(view,/supportMaterialPool/);assert.match(view,/pooledSupportMaterial/);assert.match(view,/proceduralWaterMaterial/);assert.match(view,/streamWaveA/);assert.match(view,/archiveBump/);assert.match(view,/archiveJoint/);assert.match(view,/AgXToneMapping/);assert.match(view,/skyUniforms/);assert.match(style,/clean-capture/);});
test('precision review exposes read-only pixel diagnostics for failed facade evidence',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/__archiveReviewPick/);assert.match(view,/raycaster\.intersectObjects/);assert.match(view,/familyId/);assert.match(view,/heroZone/);});
test('precision hero material state preserves occupied depth by day and hierarchy at night',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/transmission:\.28/);assert.match(view,/opacity:\.78/);assert.match(view,/night\?2\.45:\.20/);assert.match(view,/night\?1\.05:\.02/);assert.match(view,/setSupportMaterialState/);assert.match(view,/heroAtriumLights/);assert.match(view,/\[-329\.5,6\.8,50\.2\]/);});
test('precision hero uses a plaza-level lobby camera and camera-focused cinematic shadows',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/position:\[-300,3\.95,-18\]/);assert.match(view,/target:\[-325,4\.2,-60\]/);assert.match(view,/performanceMode\?1024:4096/);assert.match(view,/lightingTarget=new THREE\.Vector3/);assert.match(view,/sun\.target\.position\.copy\(lightingTarget\)/);});
test('precision hero aerial frames the actual water-plaza massing hierarchy',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/hero-archive-aerial/);assert.match(view,/position:\[-80,130,110\],target:\[-250,18,0\]/);});
test('precision performance mode keeps a camera-bounded district context without changing cinematic evidence',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/performanceMode\?\.28/);assert.match(view,/preset>=35/);assert.match(view,/performanceFocus=new THREE\.Vector3/);assert.match(view,/performanceRadiusM=reviewHeroCamera\?220:Infinity/);assert.match(view,/complete Hero GLB plus only the surrounding/);assert.match(view,/includePerformanceInstance/);assert.match(view,/includePerformanceHero/);assert.match(view,/loadedHeroZoneIds/);assert.match(view,/activeSupportInstances/);assert.match(view,/if\(!performanceMode\)controls\.update/);assert.match(view,/renderer\.setSize\(w,h,true\)/);});
test('precision hero daylight uses photographic contact depth without outline-heavy AO',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/radius:\.92/);assert.match(view,/thickness:\.74/);assert.match(view,/gtao\.blendIntensity=\.62/);assert.match(view,/new THREE\.HemisphereLight\(0xeaf3f5,0x29322d,\.38\)/);assert.match(view,/toneMappingExposure=1\.05/);assert.match(view,/sun\.intensity=4\.85/);assert.match(view,/0x23689d/);assert.match(view,/proceduralGlazing/);assert.match(view,/archiveGlassVariation/);assert.match(view,/hero-water-cinematic/);assert.match(view,/ior:1\.333/);});
test('precision hero vegetation uses procedural tonal depth',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');assert.match(view,/proceduralSurface\(new THREE\.MeshStandardMaterial\(\{name:'foliage-deep'/);assert.match(view,/foliage-mid',\.16,\.52/);assert.match(view,/foliage-light',\.14,\.48/);});
test('precision expansion loads all-zone cameras lights and wall-first support revision',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');for(const token of ['ledger-transit-corridor','hero-s-ledger-terrace','hero-s-transit-junction','position:\\[41,4\\.45,-18\\]','expandedNodeLights','expandedRouteLights','v18-metropolitan-aaa-envelope','performanceUri','performanceLod','preconsolidatedByMaterial','performance-family','performance-generic-','hero-water-cinematic','archive-procedural-water-v3-depth-edge'])assert.match(view,new RegExp(token));assert.match(view,/performanceMode&&needsPerformanceConsolidation\(hero\.scene\)\)consolidatePerformanceRoot\(hero\.scene\)/);});
test('precision performance loader publishes phase liveness without claiming ready',()=>{const view=fs.readFileSync(new URL('../src/core-district-review.ts',import.meta.url),'utf8');for(const phase of ['FAMILIES','SUPPORT_ENTRIES','HERO_GLBS','RENDER_SETUP'])assert.match(view,new RegExp(`CORE3D_LOADING\\\\|${phase}`));});
